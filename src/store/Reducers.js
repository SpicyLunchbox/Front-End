import {} from './actions'; // add actions here

const initialState = {} // add initial state here

export default function reducer(state = initialState, action) {
    switch(action.type) {
        // add cases here
    }
}